[workspace]
members = ["fastmob-core", "fastmob-py", "fastmob-rs", "fastmob-vis"]
resolver = "2"

[profile.release]
debug = "line-tables-only"
lto = "fat"
codegen-units = 1

[profile.fast-release]
inherits = "release"
lto = "thin"
codegen-units = 16
