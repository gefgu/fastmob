# v0.3.2

- `fastmob[vis]` now resolves `fastmob-vis` within its compatible `0.1.x`
  release line.
- `fastmob.vis` is the stable public visualization facade and now exposes the
  literature motif constants used by downstream applications.
